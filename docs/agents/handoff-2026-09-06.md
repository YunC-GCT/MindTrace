# 交接报告 — 2026-09-06 (复赛冲刺 · 拆分收口 + 工具化 + 演示准备)

> **给谁**: 下一个 session 的 agent。
> **先做**: `git pull` (并行会话仍在合 PR, 本文档写就后远程已前进) → 读 AGENTS.md → CONTEXT.md → [`docs/specs/index.md`](../specs/index.md) 的 Coverage map 与 Remaining 列表 (唯一权威的进度源, 本文不复述)。
> **身份**: 本文是时间点交接, 完成使命后移入 `docs/legacy/`。与代码/索引冲突时, 以代码和 index 为准。

---

## 1. 一屏现状

复赛冲刺 (09-05~08) 代码与文档侧**全部收口**, 清点报告的四项冲刺序全关:

- 拆分: [spec 015](../specs/015-knowledge-model-decomposition-v2.md) PR1-3 完成。**KnowledgeModel = 554 行轻量编排 agent** (名字经用户裁决保留), 直连两个协作服务 (PromptBuilder 49 行 / TruthCheckService 283 行); 提取全程**纯搬运**, 字节级 diff 证据在 PR #40/#41。
- 工具化: [spec 014](../specs/014-tool-calling-protocol.md) 已实现 (LlmTypes 协议字段 + ToolRegistry + ToolLoop, 在 `common/src/main/ets/tools/`)。
- Kit: ReminderFacadeImpl 已实现并组合根注入 (`entry/src/main/ets/kit/`); **UI 入口按用户裁决不挂** — 守门测试反向锁定, 勿擅自加回。
- 演示: [`demo-script-2026-09-06.md`](./demo-script-2026-09-06.md) (赛前 7 项 + 5 分钟 8 步 + 故障口径)。

## 2. 唯一待定验证 (用户标记 TBD)

**AI 对话 → 笔记真实入库, 真机未验。**

- 静态证据已闭环: 双轴审查 (bedecd7...HEAD) 确认持久化链路六个关键文件零触碰, `toKnowledgeUnit` 21 字段完整, 报告见 PR #44 相关 commit。
- **完成判据**: 真机按 [demo-script §2](./demo-script-2026-09-06.md) 步骤 3 (对话生成笔记) 走通, 最近笔记列表出现该条且重启后仍在。
- 若失败: 先抓 hilog 里 `[KnowledgeModel]` / `STRUCTURE_ERROR` 标签; 排查顺序 = 通知/网络权限 → LLM key → 结构化报错原文。**不要**先怀疑拆分 — 调用点 diff 已逐行核实。

## 3. 已知坑 (查不到、只能靠交接的)

1. **共享 worktree 互斥** (红线 7, PR #34 教训): 多 session 并行必须各自独立 worktree 或串行 — 本日发生过两次并行会话互踩 (分支被切、文件被扫入)。开工前 `git status` + `git log --oneline -3` 核实现场。
2. **hvigor CLI 环境** (教学文档 §6.11 有占位版): `DEVECO_SDK_HOME`/`JAVA_HOME` 指向 DevEco 安装目录, 且 **jbr/bin 必须进 PATH** (否则 spawn java ENOENT); 改环境后 `--stop-daemon`。DevEco 实际安装路径因机器而异, 可从开始菜单快捷方式目标反查。
3. **SDK API 24 归属异常**: `reminderAgentManager` 在 `@kit.BackgroundTasksKit` (非 NotificationKit); 其 wantAgent 字段是 `{pkgName, abilityName}` 字面量, 不需要 wantAgent.getWantAgent。**以 SDK d.ts 为准, 不信记忆/文档**。
4. **提取纪律**: 用户要求重构类搬运必须**纯提取** — 提供字节级 diff 证据 (类体 diff 只允许类名/调用直连/转发移除三类差异), 提示词与常量内容一字不改。
5. **命名裁决史** (勿再翻案): LangGraph = 采纳的设计根, ArkTS 自建承载 (ADR-0008); `mcp/` = MCP 语义工具目录, OcrTool 留守 (ADR-0010 supersede 0005); `skill/` = 小艺预留位 (ADR-0011); KnowledgeModel 保留编排 agent 之名 (ADR-0006 修订)。
6. **Hypium 只能 DevEco GUI 跑**, CLI 不可用; agent 侧新测试交付后需请用户跑。

## 4. 下一步 (按序, 详见 specs/index Remaining)

1. 用户真机验收 §2 待定项 (人工)。
2. **#7 AgentChatService 拆分** (spec 007): 并行会话已开出 PR1 (IntentClassifier 抽出, 已合入) — 接手前先读该 PR 与剩余 ticket, 沿 spec 015 同款模式 (纯提取 + 分阶段守门)。
3. F1 P1 只读工具若仍有缺口: note_get / review_due_query (note_query 已由并行会话落地)。
4. 赛后打磨: 审查判断项清单 (双入口 Middle Man / normalize 哨兵三重复 / StudyPlanService 走 LlmCaller seam / tool_choice 字面量类型), 见 PR #44 双轴审查记录。

## Last updated

2026-09-06 (复赛窗口 Day 2 收工交接)
